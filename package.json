{
  "name": "payment_city_vehicles",
  "version": "1.0.0",
  "description": "Backend for sending Telegram messages securely.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --platform=browser",
    "postinstall": "node -e \"const fs = require('fs'); if (fs.existsSync('.env.example') && !fs.existsSync('.env')) { fs.copyFileSync('.env.example', '.env'); console.log('.env file created from .env.example.'); }\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^2.7.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
