{
  "name": "transport-payment-backend",
  "version": "1.0.0",
  "description": "Backend for sending Telegram messages securely.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --platform=browser",
    "postinstall": "node -e \"const fs = require('fs'); if (!fs.existsSync('.env')) { fs.copyFileSync('.env.example', '.env'); console.log('.env file created from .env.example. Please update it with your credentials.'); }\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
